<%= form_with(model: workout_category) do |form| %>
  <% if workout_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(workout_category.errors.count, "error") %> prohibited this workout_category from being saved:</h2>

      <ul>
        <% workout_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
