= link_to(t(".back"), gyms_path, class: "mb-3 btn btn-primary")
%h1.mb-3= @gym.name
.d-flex.flex-wrap.justify-content-between.mb-3
  .d-flex.align-items-center
    %h3.mb-0.me-2= Gym.human_attribute_name("equipment")
    %span.badge.rounded-pill.bg-secondary.px-2
      .px-2
        = @gym.equipment.count
%ul
  - @gym.equipment.each do |equipment|
    %li= equipment.name
