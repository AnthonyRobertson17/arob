<%= turbo_frame_tag(@wishlist_item) do %>
  <div class="mb-3">
    <h1 class="mb-3"><%= t(".title") %></h1>
    <%= render("form", wishlist_item: @wishlist_item, wishlist: @wishlist) %>
  </div>
<% end %>
