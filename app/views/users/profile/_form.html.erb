<%= simple_form_for(current_user, method: :patch, url: "/profile" ) do |f| %>
  <p>
    <strong>Email:</strong>
    <%= current_user.email %>
  </p>
  <%= f.input :time_zone%>

  <%= f.button :submit %>
<% end %>
