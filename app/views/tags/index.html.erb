<div class="d-flex mb-3 align-items-end">
  <h1 class="h1 mb-0 me-3"><%= t(".title") %></h1>
</div>

<% if @workout_tags.any? %>
  <h3 class="h3"><%= t(".workout_tags") %></h3>
  <div id="workout_tags">
    <% @workout_tags.each do |workout_tag| %>
      <div class="card mb-3">
        <div id="<%= dom_id workout_tag %>" class="card-body">
          <%= render "tag", tag: workout_tag %>
          <%= link_to t(:open), workout_tag, class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
<% end %>

<% if @exercise_type_tags.any? %>
  <h3 class="h3"><%= t(".exercise_type_tags") %></h3>
  <div id="exercise_type_tags">
    <% @exercise_type_tags.each do |exercise_type_tag| %>
      <div class="card mb-3">
        <div id="<%= dom_id exercise_type_tag %>" class="card-body">
          <%= render "tag", tag: exercise_type_tag %>
          <%= link_to t(:open), exercise_type_tag, class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
<% end %>
